# self — User Model

Who you are: identity, standards, preferences, working style. The profile that compounds over time. [shared — all agents read]

Atomic notes. **`memory_type: semantic`** (the primary axis); `type: self` is the topic label. Files use
the `UPPERCASE.md` convention (matching `AGENT.md`); only index/template files keep the `_` lowercase form.

Layout (written at **genesis**, then grown):
- `IDENTITY.md` — the orchestrator's identity: its name, the user's name, its voice
- `CONSTITUTION.md` — the user's forked law, with their values-seed woven into Part 1
- `PROFILE.md` — the user model: who the user is, how they work (grows over time)

<!-- mem-links:start (auto-maintained — edit above this line) -->
## Map
_(empty — notes will appear here as they're written)_
<!-- mem-links:end -->
