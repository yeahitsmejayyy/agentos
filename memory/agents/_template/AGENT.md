# <Agent Name>

> **Frame.** A **grown** worker's definition. It lives *here, in the mind* —
> `.mem/agents/<name>/AGENT.md`, beside this agent's own memory (`PROFILE.md`, `skills/`, `scratch/`) —
> **not** in the shipped seed repo. That's the seed-vs-instance line: the shipped agents (orchestrator,
> watcher) define their RISEN in the repo's `agents/`; the team **you grow** defines it here, portable
> with your mind and outside the harness. Written by the `create-agent` skill (or the Watcher's
> `add-agent`). Fill the RISEN sections below; use `{{USER_NAME}}` / `{{ORCHESTRATOR_NAME}}` for identity
> that recall fills live. Every agent answers to `/shared/CONSTITUTION.md`.
>
> **RISEN** each closes off a failure mode: **Role** prevents generic output, **Instructions** prevent
> ambiguity about the deliverable, **Steps** prevent skipped phases, **End Goal** keeps work aligned,
> **Narrowing** controls tone, format, and scope.

## Role
Who this agent is — its domain, expertise, and point of view. One or two sentences, specific enough to
rule out generic answers. **Single-purpose: one job.**

## Instructions
What this agent produces — the deliverable(s) and the standard they're held to.

## Steps
How it works through a task, in order — the repeatable process it follows.

## End Goal
Why the work matters — the outcome that defines success.

## Narrowing
The boundaries: tone, format, length, scope, and what to never do.

*Clauses reinforced from the CONSTITUTION: <the ones most relevant to this agent's role>.*
