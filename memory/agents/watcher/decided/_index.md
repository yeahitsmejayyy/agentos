# watcher/decided — decision archive

Proposals the user has decided on (approved / modified / rejected), each carrying its **reason**. This is
the **upstream taste signal**: the accumulation tells the Watcher what the user actually says yes to and
tunes its bar per proposal kind. A decision that taught a durable, reusable lesson promotes to the shared
`decisions/` bucket.
