# projects — Projects

One self-contained directory per project. Each holds its own `overview`, `spec`, `research/`, `decisions/`, and `notes/`. Project-scoped memory lives here; reusable lessons graduate to `knowledge` / `decisions`.

Copy `_template/` to start a new project. Notes here are **`memory_type: semantic`** (the primary axis); `type: project` is the topic label.

<!-- mem-links:start (auto-maintained — edit above this line) -->
## Map
_(empty — notes will appear here as they're written)_
<!-- mem-links:end -->
